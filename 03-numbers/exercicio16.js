// 16. Crie uma função que receba dois números como parâmetros e retorne a diferença entre eles.

function diferenca(numero1, numero2) {
  return `A diferença entre ${numero1} e ${numero2} é de: ${numero1 - numero2}`;
}

console.log(diferenca(10, 3));
