// 8. Verifique se uma string termina com um ponto final.

let string = "A frase com ponto final";

let resultado = string.endsWith(".")
  ? `A string termina com '.' final`
  : `A string não termina com '.' final`;

console.log(resultado);
